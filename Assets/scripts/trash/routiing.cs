using UnityEngine;

public static class routiing
{
    public static bool isExecute = false;

    public static bool isDead = false;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
}
